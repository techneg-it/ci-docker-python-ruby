---
driver:
  name: docker

provisioner:
  name: dummy

verifier:
  name: shell

platforms:
  - name: test
    driver:
      image: dafyddj/ci-docker-python-ruby:local-test
      platform: debian

suites:
  - name: default
